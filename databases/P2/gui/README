Compile with:

    javac *.java

Then run with:

    java -cp mariadb-java-client-3.3.3.jar:. BirdGui <your MariaDB username> <your MariaDB password>

Note that you might have to escape some characters in your MariaDB password with backslashes.  For example, any ! needs to be replaced by \!

Please update this README as needed.
